name = "finance-os"
compatibility_date = "2024-09-23"
pages_build_output_dir = "."

[[d1_databases]]
binding = "DB"
database_name = "finance-os-db"
database_id = "62ca192e-7dbc-4a37-946c-c7d9c858816e"

# Secrets — set via CLI once, never commit:
# wrangler secret put SESSION_SECRET
#   generate value with: openssl rand -hex 32
